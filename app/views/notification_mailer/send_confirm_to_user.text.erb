<%= @current_user.name %> 様

以下の商品を購入しました。
商品の到着まで、しばらくお待ちください。

<% @current_cart.line_items.each do |item| %>
  商品名：<%=item.good.name%>
  商品価格：<%= item.good.price %>円
  数量：<%= item.quantity%>　個
  小計：<%= (item.total_price).to_i %>円

<% end %>
総計：<%= (@current_cart.total_price).to_i %>円
________________________________________________________
Shopping for everyone
